# XT-NetRC 多人协作与上传规范

## 1. 适用范围

本规范适用于 XT-NetRC 树莓派智能车工程：

- 本地目录：`/Users/yuhaojin/Documents/5G_opencv`
- GitHub 仓库：`https://github.com/kxkxkxa767/stupid_car.git`
- 默认分支：`main`
- 树莓派原工程：`/home/5G/5G`，禁止覆盖
- 树莓派测试目录：`/home/5G/xtnetrc_staging/`

本文中“上传”指向 GitHub 执行 `git push`；向树莓派传送交叉编译产物称为“部署”。

## 2. 强制原则

1. **每次开始修改前，必须先检查工作区，并拉取一次远端 `main`。**
2. 工作区不干净时禁止盲目 `git pull`；先确认修改归属，完整修改可在 `main` 上提交，
   未完成修改使用带说明的 `stash` 短期暂存。
3. **本项目默认直接在 `main` 上进行小步修改、提交和推送，不为每次修改单独创建功能分支。**
   GitHub 和本地提交历史可用于查看差异与恢复旧版本；减少短期功能分支可以避免多人额外合并。
   只有大型实验、长期未完成工作或负责人明确要求隔离时，才创建功能分支。
4. 一个提交只处理一个明确问题，不混入无关文件、本地环境或大型二进制文件。
5. 禁止重新初始化仓库，禁止对共享分支强制推送，禁止使用 `git reset --hard` 或 `git checkout --` 丢弃他人修改。
6. 不覆盖树莓派原有且已验证可用的视觉程序；新程序只部署到 staging 目录。
7. 推送前必须再次同步远端并执行相关测试，不得依赖“在我电脑上能跑”。

## 3. 每次修改前的必做步骤

### 3.1 工作区干净时

```bash
cd /Users/yuhaojin/Documents/5G_opencv
git status --short --branch
git switch main
git pull --ff-only origin main
git log -3 --oneline --decorate
```

`git pull --ff-only` 成功后，才能开始本次修改。直接留在 `main` 上工作，不再默认执行
`git switch -c ...`。修改应拆成范围明确、便于从提交历史恢复的小提交。

### 3.2 工作区不干净时

`git status --short` 有输出时，先停止新修改，确认这些文件属于谁。属于自己的完整修改可在
`main` 上检查并提交：

```bash
git add <明确的文件路径>
git diff --cached
git commit -m "<模块>: <本次完成的事项>"
```

如果修改还不具备提交条件，可短期暂存：

```bash
git branch --show-current
git stash push -u -m "<姓名>-<日期>-<原因>"
git switch main
git pull --ff-only origin main
git stash pop
```

`stash pop` 后必须立即处理冲突并检查 `git status`。不允许长期把工作隐藏在 stash 中。

### 3.3 确需使用独立分支时

大型实验、长期工作或负责人明确要求时才使用独立分支。即使是继续昨天的任务，
修改前也要先更新 `main`，再把最新内容整合到功能分支：

```bash
git status --short --branch
git switch main
git pull --ff-only origin main
git switch <功能分支>
git rebase main
```

如果 `git rebase` 出现冲突，不得直接选择“全部保留我方”或“全部保留对方”。先查看两边改动目的，必要时联系对应修改人。

## 4. 修改期间的文件管理

### 4.1 禁止加入仓库的内容

下列内容应保持在 `.gitignore` 之外，提交前仍必须人工复查：

- `.venv/`、`venv/`、`env/`
- `build/`、`local/`、`third_party/`
- `backup/`、`tmp/`
- Mac 或 ARM64 交叉编译链、sysroot、编译中间文件
- 从树莓派备份下来的完整原车目录
- 大型视频、无关截图、日志、缓存和临时文件
- Wi-Fi 密码、SSH 私钥、Token、云服务密钥、个人账号或支付凭据

检查某文件是否已被忽略：

```bash
git check-ignore -v <文件路径>
```

禁止为了上传单个编译产物而使用 `git add -f`。

### 4.2 高风险文件

修改下列内容前需在群内说明原因，提交中附带实测数据：

- `main/config/vehicle_2023.json`
- `main/camera_calibration/output/camera_intrinsics.json`
- `main/camera_calibration/output/ground_projection.json`
- 转向中心、电调中性、电机 PWM 范围和 GPIO 配置
- 安全停车、失线处理、超时和硬件使能逻辑

固定摄像头身份须按最新标定记录核对，不得将历史 `camera 0` 永久等同于固定镜头。
2026-09-06 用户确认 H65 画面用于固定摄像头标定，其 by-id 为
`/dev/v4l/by-id/usb-H65_USB_CAMERA_H65_USB_CAMERA-video-index0`，本次对应 `/dev/video2`。
只使用 `640x480` 原图及匹配该设备、姿态和分辨率的 `ground_projection.json`；拔插后
重新检查 by-id 映射。当前四点为后轴前方 `1.50/2.00 m`、左右 `±0.25 m`，尚待独立
实测精度验证。`ground_projection_before_*.json` 和 `ground_projection_superseded_*.json`
均为历史备份，禁止直接恢复成当前参数。详见 `main/camera_calibration/README.md`。

### 4.3 原车和外来代码

- 树莓派 `/home/5G/5G` 中已验证可用的视觉程序只读保留。
- 外来 `code/` 中的案例只用于参考，未经代码审查、参数适配和安全测试禁止在车上运行。
- 不得为了方便而在新程序中自动删除原车文件、停止无关服务或结束无关进程。

## 5. 提交前检查

先查看所有改动：

```bash
git status --short
git diff --check
git diff --stat
git diff
```

只添加本次需要的文件，不要使用 `git add .`：

```bash
git add <文件1> <文件2>
git diff --cached --check
git diff --cached --stat
git diff --cached
```

确认暂存区只有本次任务后再提交：

```bash
git commit -m "<模块>: <动作与结果>"
```

提交信息示例：

```text
vision: 增加人行横道停车检测
control: 失线时立即回到电调中性
calibration: 更新加固后的固定相机外参
docs: 增加比赛联调流程
```

## 6. 测试要求

提交前至少执行与改动相关的测试，并在提交说明或合并请求中记录命令和结果。

Mac 通用构建与测试：

```bash
cmake --preset mac-release
cmake --build --preset mac-release
ctest --test-dir build/mac-release --output-on-failure
```

Python 视觉测试：

```bash
.venv/bin/python -m unittest discover -s OpenCV/python/tests -v
```

树莓派 ARM64 程序必须在 Mac 上交叉编译，编译产物不加入 Git：

```bash
./scripts/build_pi_motion_cross.sh
./scripts/build_pi_vision_cross.sh
./scripts/build_pi_visual_distance_test.sh
```

不要在树莓派原工程内编译或覆盖文件。实车测试前必须先完成无电机输出的 dry-run，再进行架空测试和地面测试。

## 7. 直接推送 `main` 前再同步

在 `main` 上完成本次小步提交后，推送前再次同步远端：

```bash
git status --short --branch
git fetch origin
git rebase origin/main
```

如果出现冲突，解决后重新运行测试。确认工作区干净：

```bash
git status --short --branch
git log --oneline --decorate --graph -8
```

确认测试通过后直接推送 `main`：

```bash
git push origin main
```

如果其他人已先推送，必须先 rebase 并重新测试，不能覆盖对方提交。Git 历史虽然可以恢复
旧版本，但仍禁止对 `main` 使用 `--force` 或 `--force-with-lease`。需要代码审查时可对大型
独立分支创建 Pull Request；日常小步修改不要求额外建分支。

## 8. GitHub 代理处理

默认不保留 Git 代理。如 GitHub 连接失败，先读取 macOS 当前代理：

```bash
scutil --proxy
```

根据输出中实际启用的 `HTTPProxy/HTTPPort`、`HTTPSProxy/HTTPSPort` 或 `SOCKSProxy/SOCKSPort` 临时设置仓库级代理。下面的主机和端口只是占位符，必须换成 `scutil --proxy` 显示的实际值：

```bash
git config --local http.proxy http://<代理主机>:<端口>
git config --local https.proxy http://<代理主机>:<端口>
git pull --ff-only origin main
git push
```

上传完成后立即清除仓库代理：

```bash
git config --local --unset-all http.proxy
git config --local --unset-all https.proxy
git config --local --get-regexp '^(http|https)\.proxy$'
```

最后一条命令应无输出。不修改全局 Git 代理，不把代理端口写进脚本或提交到仓库。

## 9. 推送检查清单

推送人逐项确认：

- [ ] 修改人在开始任务前已拉取最新 `origin/main`
- [ ] 本地 `main` 在推送前已基于最新 `origin/main` 整合
- [ ] 提交只包含本次任务所需文件
- [ ] 未加入虚拟环境、编译链、sysroot、构建产物、备份和秘钥
- [ ] 未覆盖树莓派原有可用视觉程序
- [ ] 标定文件、GPIO、PWM 或安全逻辑的改动有实测根据
- [ ] 所有相关测试通过，已记录测试命令和结果
- [ ] 冲突已由涉及的修改人复核，没有盲目丢弃任意一方代码
- [ ] 实车部署目标仅为 `/home/5G/xtnetrc_staging/`
- [ ] 临时 Git 代理已清除

## 10. 实车部署最小安全流程

1. Mac 本地测试通过。
2. Mac 交叉编译 ARM64 程序。
3. 检查二进制文件架构和动态库依赖。
4. 只上传到 `/home/5G/xtnetrc_staging/<模块名>/`。
5. 先执行 `--help`、动态库加载和无电机输出预检。
6. 车辆架空、清空周围、准备物理断电后，才允许测试舅机和电机。
7. 地面测试按短距离、低 PWM、低速到完整任务逐级扩大。
8. 测试后确认电调回中性、GPIO 释放、摄像头释放且无残留进程。
