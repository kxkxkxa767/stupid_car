# XT-NetRC 当前调试进度

更新时间：2026-09-06

## 1. 当前结论

工程仍采用 Mac 本地开发、测试和 ARM64 交叉编译，树莓派只执行
`/home/5G/xtnetrc_staging/` 中的测试文件。树莓派原工程 `/home/5G/5G` 未修改。

目前已完成传感器识别、固定相机标定复核、舵机中点标定、低速电机测试、GPS/IMU
只读采集和双实线循迹原型。距离控制与夜间循迹仍处于实验阶段，尚未达到比赛可用状态。

### 本轮结构整合（2026-09-06，仅本地）

- 五项结构问题已落实：相机身份绑定、统一运行入口、共享硬件适配层、独立命令
  看门狗、根构建/测试整合。新入口是 `xtnetrc_car_runtime`，默认不输出电机信号。
- 按用户要求增加显式相机身份不匹配降级模式；编号变化但设备身份相同不拦截。
  降级目标速度最多 0.10 m/s；分辨率、数据时效、急停和超时保护不能跳过。
- 循迹 → 米制路径 → Pure Pursuit → GPS 速度 PI → 标定映射 → 安全输出已连接。
  控制线程 20 Hz，GPS PI 默认 1 Hz；IMU 轴/偏置未验收，不用于驱动速度预测。
- 旧视觉距离命令改为只读，旧 GPS 固定 PWM 命令停用，旧循迹名称兼容新入口。
  不再支持旧 `--steering-check-only`/`--motor-pwm` 等参数，历史代码留在 Git。
- 当前运行配置保留中点 72，限位 70～73；前馈、角度斜率和地面精度均标记未验收，
  并未把旧实测结果冒充成新框架实车验证。
- 新增离线硬件模拟、旧帧/掉线/超时和集成测试。已补充 `workflow` 权限，
  GitHub CI 配置纳入本次提交；push / pull request 自动运行 Ubuntu 有视觉/无视觉
  两套构建和 CTest，关闭 GUI 与真实电机后端。云端结果见
  [GitHub Actions](https://github.com/kxkxkxa767/stupid_car/actions/workflows/ci.yml)，
  不与本地测试或实车验收混淆。
  本轮没有部署或启动电机。测试明细与当前命令见 `main/car_runtime/README.md`。
- 本轮结果：Mac CTest 12/12、Python 3/3、无 OpenCV 核心 5/5、视觉独立构建
  10/10 通过；ARM64 根工程交叉编译成功。沙包图只读回放没有通过双线预检，
  未输出任何电机信号；新框架实机采集和实车效果尚未验收。

后续应先验收白天视觉、GPS 静止噪声、PWM—速度及转角映射，再进行架空与短距离
测试。显式降级只解决“可控地继续调试”，不是比赛全程完成保证；人工接管协议尚待实现。

## 2. 硬件与设备映射

| 设备 | 当前确认结果 |
|---|---|
| 当前标定的固定画面 | H65 by-id，2026-09-06 r2 拍摄时是 `/dev/video2`；640×480，后轴前方 1.50/2.00 m 四点外参 |
| 另一台摄像头 | XWF，2026-09-06 记录中为 `/dev/video0` 且由 8080 展示；不得默认套用 H65 外参 |
| GPS/三轴 IMU | `/dev/ttyUSB0`，9600 baud，WIT 11 字节协议 |
| 转向舵机 | BCM GPIO 12，50 Hz，range 1000 |
| 电调 | BCM GPIO 13，200 Hz，range 40000，中性 PWM 10000 |

以上是记录中的映射，不是本轮重新连接后的实时结论。video 编号可变，现场须检查
`/dev/v4l/by-id/` 并遮挡确认物理镜头。新入口直读 V4L2，不依赖 8080 服务。

## 以下为截至 2026-09-04 的历史实测

保留历史现象与数据；本节中的“当前版本”、入口参数和测试门限均指当时版本，
不能当作 2026-09-06 统一入口的使用说明。

## 3. 舵机与电机实测

- 转向零点线性插值结果为 PWM `72.446`；当前整数控制推荐 `72`。
- PWM 增大使车辆左转，PWM 减小使车辆右转。
- 肉眼检查 PWM 72 时前轮基本朝正前方，但整数分辨率、连杆回差和载荷仍会带来小误差。
- 新版车辆驱动在启动前写入机械中点 72；停车时先把电调恢复到 10000，再把舵机恢复
  到 72 并等待 300 ms。
- 新增 `--steering-check-only`：只检查舵机位置，电调保持 10000，绝不启动电机。
- 当前地面上 PWM `11100` 可正常起步。一次 5 秒实测前进约 3.00 m，平均约 0.60 m/s。
  该速度只适用于当次电池、载荷和地面，不能作为通用常数。

## 4. GPS/IMU 进度

已实现 WIT 帧解析、GPS 地速单位换算、GPS/IMU 融合速度估计、质量门控、PI 修正计算和
只读监视程序。室外静止时可见约 7～9 颗卫星，HDOP 约 1.5～2.9。

GPS 地速静止噪声明显，实测约 0.056～0.643 m/s。短距离电机实验中曾在车辆未移动时
虚假积分约 1.664 m；另一次运动测试因卫星数下降而提前停止，GPS 只积分约 1.794 m。
因此当前 GPS 不能单独用于精确 1 米停车，也不能对低速电机做快速闭环。它只适合在
良好卫星条件下作为约 1 Hz 的低频速度修正或安全冗余。

`xtnetrc_gps_distance_test` 是带双重电机确认、GPS 质量门控和 10 秒硬超时的实验程序，
保留用于复现实测问题；不得把它当作已验证的距离控制器。

## 5. 固定摄像头双实线循迹进度

已新增基于当前 `raw_pixel_homography` 的双边界走廊检测器和实车测试程序：

- 只使用固定摄像头 0 和 640×480；
- 分别在车辆左右侧搜索边界，限制地面宽度、中心、航向和时序连续性，避免追踪相邻跑道；
- 双线预检、双重电机确认、5 秒硬超时和连续失线停车；
- 行驶中按横向中心和航向实时调节舵机；
- 可用 `--debug-video` 保存固定相机候选线录像；
- 云台摄像头可同时录像，用于人工复核实际轨迹。

夜间实测结果：

1. PWM 11100、5 秒可稳定完成约 3 m 行驶。
2. 早期参数的一轮终点处中线仍在车身右侧约 15 cm；随后一轮车辆相对初始位置向右
   移动约 26 cm，证明纠偏方向正确。
3. 使用行驶前馈 71 的一轮用户观察为基本无左右偏；相同参数重复时又出现向左偏，
   说明夜间结果尚无重复性。
4. 调试录像显示 CLAHE/Canny 会把路砖纹理、反光和灯光形成的短线段当成边界；单帧
   中心估计曾在右 13.6 cm 到左 7.2 cm 之间跳动，估计宽度曾在 0.55～0.91 m 间变化。
5. 当前版本使用 71.5 的半步控制基准，拒绝相对启动宽度超过 0.12 m、中心跳变超过
   0.08 m 或航向跳变超过 0.20 的帧，并排除图像中的近水平短线。
6. 加入方向过滤后，当前夜间静态预检只有 `16/45` 帧有效，低于 `24/45` 安全门限。
   最近一次运动尝试在连续 10 帧几何不一致后立即停车，只移动了很短距离。

结论：安全停车逻辑有效，但夜间双实线识别尚不稳定。不能通过降低安全门限强行运行。
本次比赛在白天进行，夜间参数和结果只能作为参考。

## 6. 当前安全边界

- 所有电机测试必须同时传入 `--enable-motor` 和
  `--i-understand-vehicle-will-move`。
- 每次启动前先执行 `--steering-check-only`，由现场人员确认前轮基本回正。
- 双线预检失败、连续几何异常、摄像头失败、信号中断或超时必须立即回到电调中性。
- 固定相机位置、焦距、分辨率、裁剪或缩放变化后，现有地面外参不得继续使用。
- 云台摄像头只用于录像，禁止使用固定摄像头外参进行测距。
- 不向 Git 提交交叉编译链、sysroot、构建目录、录像、临时图片、账号或密码。

## 7. 下一步调试顺序

1. 白天在真实或等效跑道重新采集固定摄像头原始视频，确认双实线的亮度、宽度和视野范围。
2. 离线回放调试检测器，要求只锁定夹住本车的两条边界，并统计有效率、宽度方差、
   中心抖动和相邻跑道误检率。
3. 静态预检稳定通过后，执行舵机回中检查，再做 1～2 m 低速短测；保留固定相机识别
   录像和云台旁观录像。
4. 连续多轮方向一致且无误检后，再恢复 3 m 测试并标定横向误差。
5. GPS 只作为低频速度参考；精确距离仍需可靠视觉里程计、编码器或其他距离传感器。
