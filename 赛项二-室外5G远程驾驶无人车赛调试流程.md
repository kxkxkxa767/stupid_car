# XT-NetRC 赛项二调试流程

## 1. 文档目的与依据

本文供 XT-NetRC 室外 5G 远程驾驶无人车赛的开发、单项调试、分段联调和全流程彩排使用。每一阶段只有在达到本文的验收条件后，才进入下一阶段。

规则依据：

- 文件：`赛项二-室外5G远程驾驶无人车赛比赛规则（初稿）.pdf`
- PDF 页数：13 页
- 本地文件修改时间：2026-09-02 22:27:32 +0800
- SHA-256：`affa42e1710ebda2375d197d0e304c27388dfcd90adee14bb225c6189248e566`
- 本流程编写时仓库基线：`f340659`

该 PDF 是“初稿”。每次收到新版规则后，先核对文件哈希、发布日期和 QQ 群通知，再更新本流程，不得假设初稿始终有效。

## 2. 比赛任务摘要

比赛赛道基于 400 米标准田径跑道，使用跑道白色分界线作为赛道边缘，总路程约 120-150 m。起点可以在操场左侧或右侧，车辆必须按 `1 -> 2 -> 3 -> 4` 顺序运行。

比赛分两个阶段：

1. **5G 远程遥控阶段**：队员站在起点，使用 4G/5G 通信遥控车辆到程序切换区，不得跟车。
2. **自主运行阶段**：车辆在切换区停稳，启动自动驾驶程序，以蓝色挡板被移开作为发车信号，依次完成循迹、人行横道停车与播报、红绿灯停车、锥桶避障、随机停车区停车和远程扫码支付。

建议将最终程序实现为明确状态机：

```text
REMOTE_CONTROL
      |
      v
SWITCH_AREA_SAFE_STOP
      |
      v
WAIT_BLUE_BOARD_REMOVAL
      |
      v
LANE_FOLLOW
      |
      +--> CROSSWALK_STOP_AND_SPEAK --> LANE_FOLLOW
      |
      +--> TRAFFIC_LIGHT_WAIT -------> LANE_FOLLOW
      |
      +--> CONE_AVOIDANCE -----------> LANE_FOLLOW
      |
      v
PARKING_SELECT_AND_ENTER
      |
      v
FINISHED_SAFE_STOP
```

所有识别模块必须受状态约束。例如蓝色挡板在起步时是发车信号，在停车区却是入口阻挡信号，不能仅凭“看到蓝色”就触发全局动作。

## 3. 规则冲突和待确认项

### 3.1 人行横道停车时间

- 第 2.2.3 节写“停 3 秒后继续前行”。
- 第 2.2.7 节写“停 10 s 后继前行”。
- 第 2.4.2 节对“未停够 10 s”设置额外 20 s 罚时。

在组委会未给出书面澄清前，程序和彩排一律按 **10 秒**执行，建议计时目标设为 `10.2-10.5 s`，留出调度和计时误差余量，但不得因播报阻塞主控循环。

### 3.2 二维码用途

规则第 2.2.2 节规定 2 区后的 10 cm x 10 cm、45 度倾斜二维码，但当前正文没有明确说明车辆需要对该二维码执行什么算法任务。终点停车区的付款码则由参赛队员在车辆停好后远程扫码支付。

在澄清前，保留二维码图像采集和视觉接口，但不把未定义行为写进自动驾驶状态机。

### 3.3 赛道和道具最终尺寸

赛道宽度、黄色胶带与实际白线之间的距离、红绿灯工作时序、相机实际允许设置等仍应以最终版规则和赛前通知为准。

## 4. 当前工程基线与缺口

截至 2026-09-02，工程不是完整的比赛自动驾驶程序，不能直接进行全赛道运行。已有基础和待完成内容如下：

| 模块 | 当前状态 | 进入全车联调前必须完成 |
|---|---|---|
| 固定相机标定 | `camera 0` 在 `640x480` 下已有加固后新外参 | 户外检查分辨率、裁剪、曝光和镜头是否未改变 |
| 车道视觉 | Python/C++ 原型能在合成图上输出车道误差 | 支持红/蓝跑道、白色边界、弯道、阴影和户外光变 |
| 运动控制 | C++ Pure Pursuit 核心已独立实现，暂未接入正式视觉和 GPIO | 将米制路径、舵机、电调和安全状态机集成 |
| 转向中心 | 实测插值中心约 `72.446`，整数 PWM 建议 `72` | 在赛用轮胎、载荷和电池状态下复核 |
| 视觉测距 | 已更换为鸟瞰 LK 稀疏光流，本机和 ARM64 构建通过 | 先做 0.2-0.3 m 短距离实测，不得沿用旧相位相关比例 |
| IMU | 车辆规格包含 IMU，原备份代码显示 WIT 类 `0x55` 帧 | 确认实车串口、帧率、安装方向、零偏和航向符号 |
| 编码器 | 外来 `code/` 未包含编码器输入或计数逻辑 | 未确认硬件前禁止声称有轮速里程计 |
| 人行横道/红绿灯/锥桶/停车 | 尚无比赛级统一模块 | 分别完成数据集、算法、状态机接口和实车验收 |
| 语音播报 | 当前只有预留目录 | 生成最终比赛语句，并验证非阻塞播放 |
| 5G 远程控制 | 原车 `network-rc.service` 使用 8080 端口 | 验证真实 4G/5G 链路、视频延迟、控制延迟和模式切换 |

测试用黑色直线和四个沙包只是标定与短距离校验辅助物，比赛程序不得依赖它们。

## 5. 团队分工与每轮记录

### 5.1 建议角色

每次实车调试至少明确下列角色：

- **安全员**：站在车辆侧后方，只负责物理开关、断电和清空路线，不同时操作终端。
- **程序员**：确认 Git 提交、构建产物、参数和日志。
- **赛道员**：摆放道具、测量尺寸、随机化位置，记录车轮是否压线。
- **5G 驾驶员**：只负责远程阶段，比赛开始后不跟车。
- **记录员**：记录时间、成功/失败、人工干预、罚时和环境。

人员可兼任，但安全员在车辆通电运动时不得兼任程序员。

### 5.2 每轮调试必填项

```text
运行编号：YYYYMMDD-HHMM-序号
Git commit：
分支：
交叉编译产物 Build ID/SHA-256：
配置文件及 SHA-256：
树莓派主机/IP：
电池电压/剩余量：
轮胎、载荷、摄像头位置：
场地颜色：红色/蓝色
天气、光照、地面干湿：
道具位置和随机种子：
运行参数：
预期行为：
实际结果：
罚时/人工干预：
日志、截图、视频路径：
结论：通过/不通过/回退
```

每轮只改一组参数或一个算法变量。没有记录 commit 和配置的实验不得作为调参依据。

## 6. 阶段 0：代码和设备安全准备

### 6.1 Git 同步

任何人开始修改前必须先执行 [上传规范.md](./上传规范.md) 中的“每次修改前的必做步骤”。工作区不干净时禁止盲目拉取。

### 6.2 硬件安全

每天第一次通电前检查：

- [ ] 转向连杆、舅机摆臂、轮胎和轮毂紧固
- [ ] 固定摄像头没有位移，不调高或降低摄像头
- [ ] 电池无鼓包、接头无松动，电调开关状态已确认
- [ ] 车辆前方和侧方无人，地面测试有足够停车距离
- [ ] 安全员可直接操作物理电机开关
- [ ] 程序在摄像头失效、视觉丢失、超时、SIGINT/SIGTERM 和异常退出时都会回到电调中性

### 6.3 树莓派进程和原工程保护

连接树莓派后先只读检查 staging 进程：

```bash
pgrep -af '/home/5G/xtnetrc_staging'
```

如需结束残留测试，只结束已核对命令行且路径位于 staging 的 PID。不得使用宽泛 `killall`，不得修改或覆盖 `/home/5G/5G`。

## 7. 阶段 1：离线构建和回放测试

### 7.1 Mac 本地测试

```bash
cd /Users/yuhaojin/Documents/5G_opencv
cmake --preset mac-release
cmake --build --preset mac-release
ctest --test-dir build/mac-release --output-on-failure
.venv/bin/python -m unittest discover -s OpenCV/python/tests -v
```

每个识别模块都必须具有：

- 正常样例
- 临界距离/临界尺寸样例
- 阴影、反光、曝光变化和运动模糊样例
- 目标缺失和空帧样例
- 错误类别或相似物误触发样例
- 与实车同分辨率的录像回放

检测程序必须输出时间戳、类别、置信度、地面位置/偏差和状态机决策，不能只输出一个“检测到”布尔值。

### 7.2 ARM64 交叉编译

树莓派可执行文件在 Mac 上交叉编译：

```bash
./scripts/build_pi_motion_cross.sh
./scripts/build_pi_vision_cross.sh
./scripts/build_pi_visual_distance_test.sh
```

对产物记录：

```bash
file build/pi-aarch64-*/xtnetrc_*
shasum -a 256 build/pi-aarch64-*/xtnetrc_*
```

交叉编译链、OpenCV sysroot、`build/`、`local/` 和 `third_party/` 都不得提交到 Git。

### 7.3 staging 部署和无动力预检

只将产物部署到：

```text
/home/5G/xtnetrc_staging/<模块名>/
```

先运行：

- `--help`
- `ldd <程序>`
- 不启用电机的摄像头打开、静止漂移、识别和日志测试
- 异常退出测试

对距离测试程序，没有同时给出 `--enable-motor` 和 `--i-understand-vehicle-will-move` 时必须保证电机不动。

## 8. 阶段 2：传感器与执行器基线

### 8.1 固定摄像头

1. 确认本次使用的是车头固定 `camera 0`，不是云台摄像头。
2. 确认算法输入严格为 `640x480`，没有额外裁剪、非等比例缩放或数字防抖。
3. 只使用加固后的 `main/camera_calibration/output/ground_projection.json`。
4. 将当前静止画面与 `ground_reference_after_camera_fix_640x480.jpg` 对比。
5. 用四个原标定点或新的实测控制点检查投影误差。

摄像头位置、俯仰、偏航、焦距、对焦、分辨率、裁剪或缩放任一改变时，停止使用当前外参。规则明确禁止人为调高或降低摄像头。

### 8.2 8080 远程视频与资源交接

原车 `network-rc.service` 使用 8080 端口提供远程上位机。调试中分别确认：

- 5G 遥控阶段视频和控制正常；
- 遥控服务和自主程序是否争用固定摄像头、GPIO 12 或 GPIO 13；
- 切换时电调先回中性，再交接摄像头和 GPIO 所有权；
- 交接失败时保持停车，不出现两个进程同时写 PWM。

禁止让新的视觉程序自动执行宽泛 `kill` 或无条件停止系统服务。模式切换应由明确、可记录、可失败回退的切换脚本或监控进程完成。

### 8.3 IMU

1. 执行 `lsusb`、列出 `/dev/ttyUSB*` 和 `/dev/ttyACM*`，记录设备 VID/PID。
2. 不发送配置命令，先以只读方式验证 9600 波特率和 `0x55` 帧。
3. 静置至少 30 s，记录角速度零偏、航向角噪声和跳变。
4. 人工将车头左转、右转，确认正负号与车辆坐标系一致。
5. 启动电机后复测，确认磁场、振动和电源噪声没有导致航向突变。

短时 1 m 直行可使用陀螺仪航向增量闭环；不得使用加速度二次积分作为距离真值。

### 8.4 转向和电调

1. 车轮架空，电调开关关闭，验证 GPIO 初始化不会误动。
2. 转向从实测中心 `PWM 72` 开始，只做小步进检查，不直接打满舅。
3. 车轮架空后打开电调，确认中性 `10000`、前进方向和异常退出回中性。
4. 只在架空通过后做地面低速试验。当前已知 `11100` 能在旧测试场地带车起步，但必须以当天电池、地面和载荷复测，不得将它当作比赛固定值。
5. 完成 0.2-0.3 m 、0.5 m 和 1.0 m 三级直行测试，同时记录后轴距离、终点横向偏差、航向变化和惯性滑行距离。

旧的整幅鸟瞰相位相关测距在黑线场景中已出现严重高估和低估，禁止继续调整旧 `29.9` 或 `8.94` 比例用于 1 m 实车。

## 9. 阶段 3：视觉单项测试

单项测试先使用录像回放，再使用静止实车，最后才低速运动。所有模块都要在红色和蓝色跑道、顺光和逆光、晴天和阴天样本上测试。

### 9.1 蓝色挡板发车

道具尺寸为 40 cm x 50 cm，放在黄线外边界外 10 cm 的赛道中间。

调试顺序：

1. 看到挡板时保持电调中性。
2. 挡板在画面中小幅移动、倾斜或被部分遮挡时仍保持等待。
3. 挡板被移开后，连续多帧确认“稳定存在 -> 稳定消失”再发车。
4. 摄像头断开、画面卡住或从未看到挡板时不发车。
5. 随机改变挡板移除时刻，连续 10 次测试无误发和漏发。

### 9.2 赛道白线循迹

1. 在实际尺寸直道上验证左右白色分界线、赛道中心和车辆位姿。
2. 分别采集红色跑道与蓝色跑道数据，禁止只对黑线或室内白底图调参。
3. 加入弯道、磨损白线、局部遮挡、行人阴影、水渍反光和阳光直射样本。
4. 检测输出必须是车辆坐标系下的米制路径和置信度，不得将像素误差直接当成 Pure Pursuit 路径点。
5. 失线、单边线不确定或路径曲率不可实现时必须减速或立即停车，不使用上一帧转向无限前行。

验收条件：低速分别完成左起点和右起点方向的直道、进弯、稳定弯道和出弯，车轮不压线，不依赖人工摆正。

### 9.3 人行横道

人行横道由 10.5 cm x 29.7 cm 白纸条组成，白纸间隔 10.5 cm，中间白纸位于赛道中心。

1. 使用“多条横向白色结构 + 地面位置 + 时序一致性”识别，避免将起终点黄线、白色赛道边线或高光误认为人行横道。
2. 减速接近，使车头停在距横道边缘小于 30 cm 的区域内，不压斑马线。
3. 停稳后计时 10.2-10.5 s，播报：`我是<学校><队名>的智能车，请为我加油！`
4. 播报异步执行，音频失败不得卡死安全控制线程。
5. 停车时间到达且路径有效后才恢复前进，人行横道只触发一次。

验收条件：不同进场速度下连续 10 次停车均不压线，时长均不少于 10 s，播报内容正确且恢复前进无二次触发。

### 9.4 红绿灯

红绿灯尺寸为 60 cm x 17 cm x 6.7 cm，居中架设，底部离地 40 cm。车辆经过灯前 1 m 处的赛场光电传感器后，信号灯进行 3-10 s 随机倒计时，绿灯后才允许通行。

1. 使用画面中红绿灯预期高度和中心位置的 ROI，避免将红/蓝跑道或锥桶颜色当成信号灯。
2. 需求连续多帧颜色一致且灯体几何成立再转换状态。
3. 红灯、黄灯、颜色不确定、目标丢失和画面冻结一律不通行。
4. 减速停在光电传感器与红绿灯之间的停车区，车头前轮不得超过该区域。
5. 绿灯稳定确认后恢复前进，不使用写死的 3 s 或 10 s 延时猜测绿灯。

验收条件：随机产生 3-10 s 倒计时，至少 10 次测试全部在停车区内稳定停车，不抢灯，绿灯后正常恢复。

### 9.5 锥桶避障

锥桶高 8 cm，底径 7.8 cm，数量 2 个，间距不大于 3 m；蓝色赛道使用红锥桶，红色赛道使用蓝锥桶，左、中、右位置随机。

1. 分别采集两种赛道颜色、两种锥桶颜色和三种横向位置。
2. 检测锥桶底部接地点并投影到车辆米制坐标，不只依赖像素框大小猜距离。
3. 根据赛道边界和车身宽度选择可行侧，规划绕行和回归车道的完整路径。
4. 不允许因绕锥桶而压赛道白线。
5. 锥桶目标丢失或可行间隙不足时减速停车，不盲目执行旧路径。

验收条件：两种场地颜色，左/中/右三个位置，单锥桶和双锥桶组合都完成测试，车身和车轮不触碰锥桶，锥桶不被拖行。

### 9.6 停车区和支付

停车区长 1 m、宽度与赛道相同，中线将其分成上下两个停车区，裁判会在任意一个入口放置蓝色挡板。

1. 只在停车阶段判断两个入口的挡板状态。
2. 选择无挡板一侧，将停车目标设置在区域中心，为 41 cm 车长留出前后余量。
3. 使用车辆位姿、边界线和里程交叉判断，确保停稳后四个轮子都在停车区内（规则允许包含边界线，调试仍应留余量）。
4. 进入 `FINISHED_SAFE_STOP` 后锁定电调中性，不再因视觉抖动二次起步。
5. 车辆停好后，队员使用已赛前登录、已准备小额支付的手机，在 30 s 内远程微信支付 0.01 元并展示结果。

验收条件：挡板分别位于两个入口时各连续成功 10 次，四轮都在未遮挡区域内，不碰挡板；支付流程完成 3 次限时演练。

## 10. 阶段 4：5G 遥控阶段专项测试

5G 遥控阶段使用局域网或程序写死会加罚 200 s，必须真实经过 4G/5G 公网链路。

### 10.1 链路验证

- [ ] 车端和操作端不在同一局域网
- [ ] 记录 4G/5G 模块网络状态，能向裁判展示遥控而非自动程序
- [ ] 8080 远程视频可用，平均延迟、峰值延迟和卡顿已测量
- [ ] 控制指令延迟、丢包和断网后的安全行为已测试
- [ ] 驾驶员始终停留在起点，不跟车

### 10.2 遥控到自主的交接

1. 遥控车辆完整停入程序切换区。
2. 电调回中性，遥控不再输出转向/电机命令。
3. 启动自动驾驶程序，完成摄像头、IMU 和 GPIO 资源交接。
4. 自动程序进入 `WAIT_BLUE_BOARD_REMOVAL`，挡板未移开时不动。
5. 记录遥控停车到自动程序就绪的时间，优化交接，但不用两个进程同时控制来缩短时间。

自主阶段一旦开始，禁止任何遥控转向、速度或路径操作，否则规则加罚 300 s。

## 11. 阶段 5：分段联调

不得在单项模块未通过时直接跑 120-150 m 全赛道。按下列顺序逐段扩大：

1. **发车挡板 + 10 m 直道循迹**
2. **直道 + 进弯 + 出弯**
3. **循迹 + 人行横道停车 + 播报 + 恢复**
4. **循迹 + 红绿灯停车 + 随机绿灯恢复**
5. **循迹 + 单锥桶避障 + 回归车道**
6. **红绿灯 + 双锥桶随机位置**
7. **停车区入口判断 + 完整停车**
8. **5G 遥控 + 程序切换 + 蓝板发车**
9. **一半赛道全元素**
10. **完整赛道**

每个分段至少完成 5 次无人工干预运行，且没有压线、碰锥桶、抢灯、超区停车或错误状态跳转，才进入下一段。

联调时同时记录：

- 相机帧时间戳与实际 FPS
- 视觉路径、置信度和目标接地位置
- IMU 航向与角速度
- 状态机入口/出口原因
- 舅机目标、实际 PWM、电调 PWM
- 无效帧计数、超时和安全停车原因
- 网络延迟与丢包（遥控阶段）

## 12. 阶段 6：全流程比赛彩排

### 12.1 赛道和随机化

彩排场地尽量按规则尺寸搭建，并进行下列随机化：

- 起点在左侧/右侧
- 红色/蓝色跑道
- 白天不同时刻的光照和阴影
- 人行横道前进场速度小幅变化
- 红绿灯 3-10 s 随机倒计时
- 两个锥桶的间距和左/中/右位置
- 停车区左/右入口挡板

调试人员不得把随机布置提前告诉程序员或将位置写入配置。

### 12.2 计时和评分复盘

无人车前轮经过起点线开始计时。每轮记录：

```text
总运行时间：
红绿灯实际停车时间：
各项加罚时间：
有效时间 = 运行时间 + 加罚时间 - 红绿灯停车时间
支付是否成功：
是否完赛：
```

只有连续 **3 次**完成全流程、无人工干预、无比赛失败项后，才允许通过提高速度优化成绩。

## 13. 按风险排序的验收线

### 13.1 一票否决项

出现任一项时立即回退到相应单项调试：

- 自主阶段需要遥控干预
- 使用局域网假装 5G 遥控或将路线写死
- 程序异常后电机未停止
- 车辆无任务原因停止超过 20 s
- 撞倒锥桶后拖行超过 2 m
- 两个进程同时输出转向或电机 PWM
- 摄像头、IMU 或控制日志时间戳严重错位，无法复盘决策

### 13.2 优先消除的高罚时项

1. 人工干预：每次 100 s
2. 停错区、碰停车挡板或无轮在停车区：100 s
3. 持续压线/骑线超过 3 s：50 s
4. 未进行红绿灯停车：50 s
5. 未进行人行横道停车：30 s
6. 未停够 10 s：额外 20 s
7. 每次触碰锥桶：20 s

红绿灯停车时间会从有效时间中扣除，因此不应为了节省时间而抢灯。开发顺序是“先零失败、再零高罚时、最后提速”。

## 14. 故障处理与回退

### 14.1 测试中立即停止的情况

- 转向连杆抖动、卡死或发热
- 电调中性异常或程序退出后车轮仍转动
- 视频卡顿、帧时间戳倒退或固定相机被移动
- IMU 航向跳变或串口数据长时间中断
- 车辆连续向赛道边界偏移且控制未收敛
- 发现运行的二进制文件、配置或 Git commit 无法对应

### 14.2 回退方法

1. 物理关闭电机开关。
2. 保存日志、摄像头原帧和现场视频，不先覆盖参数。
3. 记录当前 commit、配置哈希和产物哈希。
4. 在 Mac 上复现或用录像回放定位，不在实车现场连续盲调。
5. 将修复保存到新功能分支，按 [上传规范.md](./上传规范.md) 完成同步、测试和审查。
6. 从上一个通过的阶段重新验证，不直接回到全赛道。

## 15. 比赛日检查表

### 15.1 出发前

- [ ] 最终规则版本和组委会澄清已核对
- [ ] 比赛 commit 已打 tag，产物和配置哈希已保存
- [ ] 树莓派 staging 中只保留明确命名的比赛版本
- [ ] 原车 `/home/5G/5G` 未被覆盖
- [ ] 电池已充满，备用电池状态正常
- [ ] 固定摄像头、转向、轮胎、连杆已紧固
- [ ] 4G/5G SIM/模块、天线、套餐和公网通信已验证
- [ ] 播报文案已替换为正确学校和队名
- [ ] 微信已登录，支付功能、小额免密/验证方式已演练
- [ ] 带齐充电器、工具、备用线材、卷尺和日志存储介质

### 15.2 上场前的无动力快检

- [ ] 摄像头 `camera 0` 画面方向正确，`640x480` 且帧率稳定
- [ ] 地面投影控制点误差无明显变化
- [ ] IMU 静置航向稳定，左/右转符号正确
- [ ] 8080 远程视频和远程控制通过真实 4G/5G 链路
- [ ] 自动程序无电机输出预检通过
- [ ] 蓝板、人行横道、红绿灯、锥桶和停车区识别快检通过
- [ ] 异常退出后电调保持中性

### 15.3 计时开始前

- [ ] 车辆在赛道中线附近，贴近发车黄线且未越界
- [ ] 遥控员、程序员、支付人员和安全联系方式明确
- [ ] 已向裁判展示当前是 5G 远程遥控而非自动运行
- [ ] 日志已启动，系统时间正确，磁盘空间足够
- [ ] 车辆周围无人、无工具、无线材

## 16. 技术手册素材留存

技术手册占 20 分，至少 5 个案例，并按创新性、实用性、复杂度和可复现性评分。调试过程中同步留存下列素材：

- 问题背景、规则要求和原始失败现象
- 算法、状态机和安全设计图
- 数据集采集条件与标注说明
- 参数、代码 commit、构建和运行命令
- 测试场地尺寸、道具摆放和环境条件
- 对照实验、量化结果、失败案例与改进过程
- 从全新环境可执行的复现步骤

手册内容和源码必须原创，引用他人方案、模型或开源代码时记录来源、许可证和本队改动，不得复制他队技术手册。
